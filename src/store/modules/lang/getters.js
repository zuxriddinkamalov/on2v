/* ============
 * Getters for the lang module
 * ============
 *
 * The getters that are available on the
 * lang module.
 */
const locale = state => state.locale
const locales = state => state.locales

export default {
  locale,
  locales
}
